解释器模式
1.介绍：
 - 提供了一种解释语言的语法或表达的方式，该模式定义了一个表达式接口，通过该接口解释一个特定的上下文
2.定义：
 - 给定一个语言，定义它的文法的一种表示，并定义一个解释器，该解释器使用该表示来解释语言中的句子
3.使用场景：
 - 如果某个简单的语言需要解释执行而且可以将语言中的语句表示为一个抽象语法树时，可以考虑使用解释器模式
 - 在某些特定的领域出现不断重复的问题，可以将该领域的问题在转化为一种语法规则下的语句，然后构架监视器来解决该语句
4.Android 中的应用：
 - 清单文件解析
5.小结
 优点：
 - 灵活的扩展性
 缺点：
 - 每一条文法都可以至少对应一个介绍起，其会生成大量的类，导致后期维护困难
 - 对于过于复杂的文法，构建起抽象语法树会显得异常繁琐